# session-matters

This repository follows the Helioy Rust workspace pattern.

Use `just` recipes for local work:

- `just check`
- `just build`
- `just test`

The v1 workspace is built pass by pass through Linear.

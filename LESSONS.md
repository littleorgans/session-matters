# Lessons

- For cross-repo path dependencies, distinguish the canonical repo checkout path from the active worktree path before editing. Commit canonical paths only; use accepted worktree code to update the dependency repo checkout when verification requires it.

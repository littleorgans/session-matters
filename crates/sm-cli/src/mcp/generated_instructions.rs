// AUTO-GENERATED by build.rs from tools.toml - do not edit
#![allow(clippy::all)]

pub const SERVER_INSTRUCTIONS: &str = "session-matters controls local Helioy agent sessions through smd.\n\nUse agent_run to spawn a session, agent_list to inspect sessions, agent_get before acting on one id, and agent_delete to terminate a session.\n\nTools:\n- agent_run: Start an agent runtime through the session-matters daemon. This v1 pass supports claude and codex runtimes, a role, and a workspace. The tool returns the persisted session record.\n- agent_list: List session records known to the session-matters daemon. Pass an id only when a narrow list response is useful; use agent_get when exactly one session is required.\n- agent_get: Get one session record by id. The tool returns an error envelope when the id is unknown.\n- agent_delete: Terminate one daemon owned agent runtime by id and return the updated session record. Defaults to SIGTERM with a five second grace period.\n- mail_send: Send durable mail to one session id. Selector addressing lands in a later pass.\n- mail_read: Read unread mail for one session id. Reads mark messages read unless peek is true.\n- mail_check: Return the unread mail count for one session id without draining mail.\n- mail_stop_check: Return the unread mail count for stop-hook decisions without draining mail.\n- nudge: Send an ephemeral nudge to one session id. The v1 in-process driver logs that the tmux gateway is unavailable and does not deliver.\n\n## Session Control Workflow\n\nUse `agent_run` to start a local agent runtime through the session-matters daemon.\nUse `agent_list` to inspect live and terminated sessions.\nUse `agent_get` before acting on one session id.\nUse `agent_delete` to terminate daemon owned sessions.\nUse `mail_send`, `mail_check`, and `mail_read` for durable session mail.\nUse `nudge` for the ephemeral notification surface.";

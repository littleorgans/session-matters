# session-matters

`session-matters` is the control plane for Helioy agent sessions.

Pass 1 provides the tracer bullet:

- `sm daemon start`
- `sm daemon status`
- `sm run claude --role general --dir test --detach`
- `sm get agents`
- `sm daemon stop`
